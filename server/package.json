{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "build/app.js",
  "scripts": {
    "build": "rimraf build && tsc",
    "preserve": "npm run build",
    "serve": "cross-env NODE_ENV=development concurrently \"tsc --watch\" \"nodemon build/app.js\"",
    "prestart": "npm run build",
    "start": "cross-env NODE_ENV=production node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gospodin Dimitrov",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.2",
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.0.0"
  }
}
